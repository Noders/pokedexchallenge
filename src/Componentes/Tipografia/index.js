import React from "react";
import "./index.css";


export const Title = (props) => {
  return <span class="tipografia title h1">{props.children}</span>;
}

export const Body14 = (props) => {
  return <span class="tipografia body14">{props.children}</span>;
}

